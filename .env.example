# BurgerBoss Environment Variables - Firebase Edition

# JWT Secret (REQUIRED - Generate a strong secret key)
JWT_SECRET=your-super-secure-jwt-secret-key-for-production

# Node Environment
NODE_ENV=production

# Firebase Configuration (PRODUCTION ONLY - Set in Vercel Environment Variables)
# Get these from Firebase Console → Project Settings → Service Accounts
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY=your-firebase-private-key-from-json-file
FIREBASE_CLIENT_EMAIL=your-firebase-client-email

# SECURITY WARNING:
# NEVER commit real Firebase credentials to GitHub!
# Set these values in Vercel Environment Variables only
# The private key should include the full key with -----BEGIN PRIVATE KEY----- headers